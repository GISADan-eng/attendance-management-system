Index: backend/middleware/auth.js
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>const jwt = require('jsonwebtoken');\r\nconst user = require('../models/user');\r\n\r\nconst protect = async (req, res, next) => {\r\n    let tokens;\r\n\r\n    if (req.headers.authorization && req.headers.authorization.startsWith('Bearer ')) {\r\n        tokens = req.headers.authorization.split(' ')[1];\r\n        const decoded = jwt.verify(tokens, process.env.JWT_SECRET);\r\n        req.user = await User.findById(decoded.id).select('-password');\r\n        if (!req.user) {\r\n            return res.status(401).json({message: 'User not found'});\r\n        }\r\n        return next();\r\n    } catch (err) {\r\n        return res.status(401).json({message: 'Not authorized, token failed'});\r\n    }\r\n}\r\n\r\nreturn res.status(401).json({message: 'Not authorized, not token'});\r\n\r\nmodule.exports = {protect}; 
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/backend/middleware/auth.js b/backend/middleware/auth.js
--- a/backend/middleware/auth.js	(revision 459d8b9a66ff645e191380d88af254579f05585f)
+++ b/backend/middleware/auth.js	(date 1787408960459)
@@ -12,11 +12,13 @@
             return res.status(401).json({message: 'User not found'});
         }
         return next();
-    } catch (err) {
+    }
+    catch(err)
+    {
         return res.status(401).json({message: 'Not authorized, token failed'});
     }
 }
 
 return res.status(401).json({message: 'Not authorized, not token'});
 
-module.exports = {protect}; 
\ No newline at end of file
+module.exports = {protect};
\ No newline at end of file
